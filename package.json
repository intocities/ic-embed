{
  "name": "ic-embed",
  "version": "0.1.0",
  "description": "Embed your Into Cities virtual tour and communicate programmatically with it.",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "author": "Durch die Stadt <support@durchdiestadt.de>",
  "license": "MIT",
  "scripts": {
    "build": "yarn run tsc && yarn build-min",
    "build-min": "browserify -p tinyify --standalone IC dist/index.js -o dist/ic-embed.min.js",
    "test": "jest",
    "lintfix": "prettier --write ."
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "browserify": "^17.0.0",
    "jest": "^27.3.1",
    "prettier": "^2.5.0",
    "tinyify": "^3.0.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  },
  "browserslist": [
    "> 2% in DE",
    "last 2 versions",
    "not dead"
  ]
}
