{
  "name": "ic-embed",
  "version": "0.0.1",
  "description": "ic-embed allows you communicate with your Into Cities virtual tour.",
  "main": "dist/ic-embed.min.js",
  "files": [
    "dist"
  ],
  "author": [
    "Durch die Stadt <support@durchdiestadt.de>"
  ],
  "license": "MIT",
  "scripts": {
    "build": "terser src/ic-embed.js --ecma 5 --compress --mangle -o dist/ic-embed.min.js",
    "lint": "eslint --ext .js --ignore-path .gitignore src",
    "lintfix": "eslint --fix --ext .js --ignore-path .gitignore src"
  },
  "devDependencies": {
    "eslint": "^6.0",
    "eslint-config-standard": "^12.0.0",
    "eslint-plugin-compat": "^3.0",
    "eslint-plugin-import": "^2.18.0",
    "eslint-plugin-node": "^9.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.0",
    "terser": "^4.0.0"
  },
  "browserslist": [
    "> 2% in DE",
    "last 2 versions",
    "ie 11",
    "not dead"
  ]
}
